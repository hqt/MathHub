@model ICollection<MathHub.Web.Models.ProblemVM.HintItemVM>
@{
    foreach (MathHub.Web.Models.ProblemVM.HintItemVM hintItemVm in Model)
    {
        @Html.Partial("Partials/_HintItem", hintItemVm);
    }
}