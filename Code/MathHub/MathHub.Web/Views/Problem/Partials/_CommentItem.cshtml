@model MathHub.Web.Models.ProblemVM.CommentItemVM

Comment Item @Model.Content - @Model.UserUsername<br/>

    @using (Html.BeginForm("AddComment", "Problem", FormMethod.Post, 
                                new { style = "display:none", id = "comment-post-form" }))
        {
            @Html.LabelFor(m => m.Content)
            @Html.TextAreaFor(m => m.Content)
            @Html.ValidationMessageFor(m => m.Content)

            @Html.HiddenFor(m => m.MainPostId)
            @Html.HiddenFor(m => m.Type)
           
         @* function postComment(formId, commentDivId) *@
            <input type="button" name="addComment" onclick="postComment('comment-post-form', 'problem-comments')" value="Send"/>
        }