@model MathHub.Web.Models.ProblemVM.AnswerItemVM

Answer Item @Model.Content<br/>

<h3>Comments</h3>
<div id="@Model.Id + 'Comments'"></div>

@* function getCommentAjax(elementId, postId, offset) *@

<button onclick="getCommentAjax('answer-comments-' + @Model.Id, @Model.Id, 0)">View more comments</button> <br />
