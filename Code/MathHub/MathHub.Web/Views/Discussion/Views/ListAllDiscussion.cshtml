@using MathHub.Web.Models.DiscussionVM
@model ICollection<PreviewDiscussionVM>
@{
    ViewBag.Title = "ListAllDiscussion";
    Layout = "~/Views/Discussion/Layouts/_DiscussionListLayout.cshtml";
}
@*<h2>ListAllDiscussion</h2>
These items are created by a loop of partial views:<br/>
@{
    
    foreach (PreviewDiscussionVM discussion in Model)
    {
        @Html.Partial("Partials/_DiscussionItem", discussion);
    }
}*@



@section LeftPanel
{
    
    @Html.Action("ProfileWidget", new { controller = "CommonWidget" })
    <hr/>
    @Html.Action("FavoriteTagWidget", new { controller = "CommonWidget" })
    <hr/>
    @Html.Action("GroupWidget", new { controller = "CommonWidget" })
    <hr/>
    @Html.Action("SubscriptionWidget", new { controller = "CommonWidget" })
}

@section RightPanel
{
    @Html.Action("PostProblemWidget", new { controller = "CommonWidget" })
    <hr/>
    @Html.Action("MySubscriptionWidget", new { controller = "CommonWidget" })
    <hr/>
    @Html.Action("NewBlogWidget", new { controller = "CommonWidget" })
}
